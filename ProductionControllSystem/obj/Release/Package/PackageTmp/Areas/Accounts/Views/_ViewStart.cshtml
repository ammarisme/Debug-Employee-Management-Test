@* Authentication customs are handled within shared cshtml *@
@{
        if (Request.Headers["X-PJAX"] != null)
        {
            Layout = "Shared/_PjaxLayout.cshtml";
        }
        else
        {
            Layout = "Shared/_Layout.cshtml";
        }
}